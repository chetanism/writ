# Milestone plan — <MILESTONE>

> **Status:** active.
> **Role:** what *structure* this milestone builds, and the gates that say it is finished.
> **Precedence:** `BRD.md` > this document > `process/SLICE-QUEUE.md` > everything else.
> **Divergence:** the queue may depart from this plan only by adding a row to §9 **and** amending
> the affected section here in the same change. Everything downstream reads this document; a plan
> that silently disagrees with the queue is worse than either being wrong.

## 1. What this milestone is for

<One paragraph. What becomes possible that was not, and what question it answers.>

## 2. Gates

> A gate is a demonstrable property of the finished milestone, not a task. Each one is checked at
> the end, by hand, against a named demo.

| ID | Gate | Demonstrated by |
|---|---|---|
| G-01 |  |  |

A gate may **close twice**: provisionally against a stand-in (a sandbox, a tunnel, a fake), marked
`◐`, and finally against the real thing. **A `◐` is not milestone-complete**, and no milestone claim
rests on one. Name the slice that re-checks every provisional close.

## 3. What gets built

<The structure — components, boundaries, the shapes everything later inherits. Point at the
foundation specs rather than repeating them.>

## 4. Foundation specs

| Spec | Fixes | Slices it shapes |
|---|---|---|
| `<AREA>-SPEC.md` |  |  |

## 5. What this milestone deliberately does not build

<With where each lands instead.>

## 6. Delivery risks

| ID | Risk | Mitigation |
|---|---|---|
| DR-1 |  |  |

## 7. Open questions

| ID | Question | Closed by |
|---|---|---|
| MQ-1 |  |  |

## 8. External tracks

> Anything with a lead time that is not code: an account, a domain, a review, a partner. One row
> each, with what it unblocks, so that when a track lands the resequencing is a lookup rather than
> a re-analysis.

| Mark | Needs | Typical wait | Unblocks |
|:--:|---|---|---|
| — | Nothing but a laptop | none | everything not listed below |

## 9. Amendments

> Every departure from this plan gets a row, and the affected section above is amended in the same
> change. An unrecorded divergence is a defect, not a shortcut.

| # | Amendment | Cause |
|---|---|---|
| X-1 |  |  |
